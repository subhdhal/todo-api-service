apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-service.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "todo-service.name" . }}
  ports:
    - port: {{ .Values.service.ort }}
      targetPort: {{ .Values.containerPort }}

